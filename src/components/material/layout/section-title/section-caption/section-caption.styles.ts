import type { SxProps, Theme } from '@mui/material/styles';

// ----------------------------------------------------------------------

/** Overline typography treatment for `SectionCaption`'s root `Box`. */
export const sectionCaptionSx: SxProps<Theme> = {
  typography: 'overline',
  color: 'text.disabled',
};
