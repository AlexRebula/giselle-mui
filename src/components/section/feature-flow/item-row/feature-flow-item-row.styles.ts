import type { SxProps, Theme } from '@mui/material/styles';

// ----------------------------------------------------------------------

/** The title+description text slot: takes remaining width next to the leading icon. */
export const itemRowTextSlotSx: SxProps<Theme> = {
  flex: 1,
  minWidth: 0,
};
