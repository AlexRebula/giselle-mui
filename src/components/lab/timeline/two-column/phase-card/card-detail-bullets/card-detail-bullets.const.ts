/** Named constants for `CardDetailBullets`. */

// ── Expanded task list ────────────────────────────────────────────────────────

/**
 * Icon size (px) for task done-toggle icons in the expanded detail list.
 * Meets minimum inline icon rule (16px). Never set below 16.
 */
export const PHASE_TASK_ICON_SIZE = 16;
